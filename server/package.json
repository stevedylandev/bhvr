{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "concurrently \"tsc --pretty --watch\" \"bun --watch run src/index.ts\" --names \"tsc,bun\" --prefix-colors \"blue,magenta\""
  },
  "dependencies": {
    "hono": "^4.7.11",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
